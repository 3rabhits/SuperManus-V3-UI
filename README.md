# SuperManus V3 - Professional Web Interface

واجهة ويب احترافية لـ SuperManus V3 مستوحاة من تصميم Manus.im

## ✨ الميزات الجديدة

### تصميم مستوحى من Manus.im
- واجهة نظيفة وعصرية بألوان فاتحة
- شريط جانبي قابل للطي مع قائمة المهام والمشاريع
- تصميم متجاوب يدعم الجوالات والأجهزة اللوحية

### لوحة الملفات (Files Panel)
- **Preview** - معاينة حية لملفات HTML داخل iframe
- **Code** - عرض الكود المصدري مع تنسيق syntax highlighting
- **Files** - قائمة جميع الملفات المُنشأة مع أيقونات حسب النوع
- زر تحميل لكل ملف
- تحديث تلقائي عند إنشاء ملفات جديدة

### واجهة الدردشة
- رسائل المستخدم والوكيل بتصميم فقاعات
- عرض خطوات المهمة القابلة للتوسيع
- حالة الاتصال في الوقت الفعلي (Connected/Disconnected)
- أزرار اختصارات سريعة (Search, Build website, Analyze data, Write code)

### اتصال WebSocket
- تحديثات فورية أثناء تنفيذ المهام
- عرض تقدم الخطوات
- إشعارات الملفات الجديدة

## 🚀 التشغيل

### المتطلبات
- Python 3.11+
- Node.js 18+
- OpenManus V3

### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

### Frontend
```bash
cd frontend
npm install
PORT=3003 npm start
```

### أو استخدم سكريبت التشغيل
```bash
chmod +x start.sh
./start.sh
```

## 📁 هيكل المشروع

```
SuperManus_UI/
├── backend/
│   ├── main.py          # FastAPI server with WebSocket
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── App.js       # React app (Manus.im style)
│   │   └── index.css    # TailwindCSS styles
│   └── public/
│       └── index.html
├── start.sh
└── README.md
```

## 🔗 الروابط

- **Frontend**: http://localhost:3003
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

## 📝 API Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/chat` | POST | بدء محادثة جديدة |
| `/api/sessions` | GET | قائمة الجلسات |
| `/api/sessions/{id}/files` | GET | ملفات جلسة معينة |
| `/api/files` | GET | قائمة جميع الملفات |
| `/api/preview/{file}` | GET | معاينة ملف HTML |
| `/api/download/{file}` | GET | تحميل ملف |
| `/ws/{session_id}` | WS | WebSocket للتحديثات الفورية |

## 🎨 التخصيص

يمكنك تخصيص الألوان والتصميم من خلال:
- `frontend/src/index.css` - الأنماط العامة
- `frontend/src/App.js` - مكونات React

## 📱 دعم الجوال

- شريط جانبي قابل للطي
- أزرار بحجم مناسب للمس
- دعم Safe Area للأجهزة ذات الشق
- منع التكبير التلقائي على iOS

## 📄 الترخيص

MIT License
