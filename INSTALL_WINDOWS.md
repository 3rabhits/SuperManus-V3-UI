# 🚀 دليل تنصيب SuperManus V3 UI على Windows

## 📋 المتطلبات الأساسية

### 1. تثبيت Node.js
1. اذهب إلى: https://nodejs.org/
2. حمّل النسخة **LTS** (الموصى بها)
3. شغّل المثبت واتبع الخطوات
4. تأكد من تفعيل خيار "Add to PATH"

**للتحقق من التثبيت:**
```cmd
node --version
npm --version
```

### 2. تثبيت Python (اختياري - للـ Backend)
1. اذهب إلى: https://www.python.org/downloads/
2. حمّل أحدث نسخة Python 3.x
3. **مهم:** فعّل خيار "Add Python to PATH" أثناء التثبيت

**للتحقق من التثبيت:**
```cmd
python --version
pip --version
```

### 3. تثبيت Git (اختياري)
1. اذهب إلى: https://git-scm.com/download/win
2. حمّل وثبّت Git for Windows

---

## 📦 طريقة التنصيب

### الطريقة 1: من ملف ZIP (الأسهل)

1. **استخراج الملفات:**
   - انقر بزر الماوس الأيمن على `SuperManus_V3_Complete.zip`
   - اختر "Extract All..." أو "استخراج الكل..."
   - اختر مجلد الوجهة (مثلاً: `C:\Projects\SuperManus`)

2. **تثبيت التبعيات:**
   - افتح Command Prompt أو PowerShell
   - انتقل إلى مجلد المشروع:
   ```cmd
   cd C:\Projects\SuperManus\frontend
   npm install
   ```

3. **تشغيل المشروع:**
   - انقر مرتين على `start.bat` في المجلد الرئيسي
   - أو شغّل يدوياً:
   ```cmd
   cd C:\Projects\SuperManus\frontend
   npm start
   ```

4. **فتح المتصفح:**
   - سيفتح تلقائياً على: http://localhost:3000

---

### الطريقة 2: من GitHub

1. **استنساخ المستودع:**
   ```cmd
   git clone https://github.com/3rabhits/SuperManus-V3-UI.git
   cd SuperManus-V3-UI
   ```

2. **تثبيت التبعيات:**
   ```cmd
   cd frontend
   npm install
   ```

3. **تشغيل المشروع:**
   ```cmd
   npm start
   ```

---

## 🖥️ تشغيل الـ Backend (اختياري)

إذا أردت تشغيل الخادم الخلفي للحصول على ردود AI حقيقية:

1. **تثبيت تبعيات Python:**
   ```cmd
   cd backend
   pip install -r requirements.txt
   ```

2. **إعداد مفتاح OpenAI (اختياري):**
   - أنشئ ملف `.env` في مجلد `backend`:
   ```
   OPENAI_API_KEY=your_api_key_here
   ```

3. **تشغيل الخادم:**
   ```cmd
   python -m uvicorn main:app --host 0.0.0.0 --port 8000
   ```

---

## 🔧 حل المشاكل الشائعة

### مشكلة: `'npm' is not recognized`
**الحل:** تأكد من تثبيت Node.js وإضافته إلى PATH:
1. أعد تشغيل Command Prompt
2. أو أعد تشغيل الكمبيوتر

### مشكلة: `EACCES permission denied`
**الحل:** شغّل Command Prompt كـ Administrator:
1. ابحث عن "cmd" في قائمة Start
2. انقر بزر الماوس الأيمن
3. اختر "Run as administrator"

### مشكلة: `Port 3000 is already in use`
**الحل:** استخدم منفذ آخر:
```cmd
set PORT=3001 && npm start
```

### مشكلة: `npm install` بطيء جداً
**الحل:** استخدم مرآة npm أسرع:
```cmd
npm config set registry https://registry.npmmirror.com
npm install
```

### مشكلة: خطأ في تحميل الصفحة
**الحل:** امسح ذاكرة التخزين المؤقت:
```cmd
npm cache clean --force
rd /s /q node_modules
del package-lock.json
npm install
```

---

## 📁 هيكل المشروع

```
SuperManus-V3-UI/
├── frontend/                 # الواجهة الأمامية (React)
│   ├── public/              # الملفات العامة
│   │   └── index.html       # صفحة HTML الرئيسية
│   ├── src/                 # الكود المصدري
│   │   ├── App.js           # المكون الرئيسي
│   │   ├── index.js         # نقطة الدخول
│   │   └── index.css        # الأنماط
│   ├── package.json         # تبعيات npm
│   └── build/               # الملفات المبنية (بعد npm run build)
├── backend/                  # الخادم الخلفي (Python)
│   ├── main.py              # الخادم الرئيسي
│   ├── config.py            # الإعدادات
│   └── requirements.txt     # تبعيات Python
├── start.bat                # سكريبت التشغيل لـ Windows
├── start-backend.bat        # سكريبت تشغيل الـ Backend
├── README.md                # وصف المشروع
└── INSTALL_WINDOWS.md       # هذا الملف
```

---

## 🌐 النشر (Deployment)

### للنشر على Vercel (مجاني):
1. أنشئ حساب على https://vercel.com
2. اربط حسابك بـ GitHub
3. استورد المستودع
4. اختر مجلد `frontend` كـ Root Directory
5. انقر Deploy

### للنشر على Netlify (مجاني):
1. أنشئ حساب على https://netlify.com
2. اسحب مجلد `frontend/build` إلى الصفحة
3. سيتم النشر تلقائياً

---

## 📞 الدعم

- **GitHub Issues:** https://github.com/3rabhits/SuperManus-V3-UI/issues
- **المستودع:** https://github.com/3rabhits/SuperManus-V3-UI

---

## 📄 الترخيص

MIT License - يمكنك استخدام وتعديل ونشر المشروع بحرية.
