# SuperManus V3 - Environment Configuration
# Copy this file to .env and modify as needed

# ============================================================================
# Paths Configuration
# ============================================================================

# Path to OpenManus_V3 installation
# If not set, will auto-detect from sibling directory or home directory
# OPENMANUS_PATH=/path/to/OpenManus_V3

# Path to workspace directory for created files
# If not set, will use OpenManus workspace or ./workspace
# WORKSPACE_PATH=/path/to/workspace

# ============================================================================
# Security Configuration
# ============================================================================

# Environment mode: development or production
# In production mode, CORS is restricted and API_KEY is required
ENV=development

# API Key for authentication (required in production)
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
# API_KEY=your-secure-api-key-here

# Allowed CORS origins (comma-separated)
# Default: localhost only
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3003,https://yourdomain.com

# ============================================================================
# Server Configuration
# ============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8000

# ============================================================================
# Limits Configuration
# ============================================================================

# Maximum file size to read content (bytes)
MAX_FILE_SIZE=50000

# Maximum number of files to scan
MAX_FILES_SCAN=1000
